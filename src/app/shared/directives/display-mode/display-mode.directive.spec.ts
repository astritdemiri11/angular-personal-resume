import { DisplayModeDirective } from './display-mode.directive';

describe('DisplayModeDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayModeDirective({} as any, {} as any, {} as any);
    expect(directive).toBeTruthy();
  });
});
